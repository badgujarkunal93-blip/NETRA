services:
  - type: web
    name: netra-suspect-priority-service
    env: python
    rootDir: priority-model-service
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health
    autoDeploy: true
